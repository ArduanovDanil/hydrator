name: Update docs translation

on:
  push:

jobs:
  update-docs:
    name: Update docs
    runs-on: ubuntu-latest
    steps:
      - name: Before checkout
        run: |
          pwd
          ls -al

      - name: Checkout
        uses: actions/checkout@v4
        
      - name: After checkout
        run: |
          pwd
          ls -al